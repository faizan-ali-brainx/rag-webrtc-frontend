/** File extensions accepted by the upload picker. */
export const ACCEPTED_EXTENSIONS = '.pdf,.txt,.md'

/** Maximum upload size in megabytes (kept in sync with the backend). */
export const MAX_UPLOAD_MB = 15

/** How often (ms) to re-poll while a document is still processing. */
export const POLL_INTERVAL_MS = 2000
